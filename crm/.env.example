# Database (SQLite file stored in prisma/)
DATABASE_URL="file:./dev.db"

# App
APP_DOMAIN=https://libconsulting-thai.cloud
SESSION_SECRET=change-me-to-a-random-string

# Set to "production" on the server
NODE_ENV=production
