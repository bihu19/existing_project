# Database (PostgreSQL — required for Vercel serverless deployment)
# For Supabase, you need BOTH URLs from Project Settings > Database:
#
# DATABASE_URL  = Transaction/Pooler connection (port 6543) — used at runtime
# DIRECT_URL    = Session/Direct connection (port 5432) — used for migrations & schema push
#
# IMPORTANT: DATABASE_URL must include &prepared_statements=false for PgBouncer compatibility
#

# App
APP_DOMAIN=https://your-app.vercel.app
SESSION_SECRET=change-me-to-a-random-string
